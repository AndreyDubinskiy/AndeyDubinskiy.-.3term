1.1 int *p = &a;
1.2 float *p = &a;
1.3 int *p = &array[0];
1.4 char *p = &str[7];
1.5 int **q = &p;
1.6 *p *= 2;
1.7 *p -= ('t' - 'T');
1.8 p += 3, *p += 1;
1.9 p -= 3, *p += 1;
1.10 while (*p != '\0') {
        if (*p >= 'a' && *p <= 'z') { 
            *p -= ('a' - 'A'); 
        }
        p++;
    }
1.11 **q *= 2;